<how-it-works-dialog id="how-it-works-dialog">
  <dialog
    class="how-it-works-modal dialog-modal color-{{ settings.popover_color_scheme }}"
    ref="dialog"
    scroll-lock
  >
    <button
      ref="closeButton"
      on:click="/closeDialog"
      class="button how-it-works-modal__close"
      aria-label="{{ 'accessibility.close_dialog' | t }}"
    >
      {{- 'icon-close.svg' | inline_asset_content -}}
    </button>
    
    <div class="how-it-works-modal__content">
      <div class="how-it-works-modal__header">
        <h2 class="how-it-works-modal__title">{{ 'preorder.how_it_works.title' | t: default: 'How preorders work' }}</h2>
      </div>
      
      <div class="how-it-works-modal__body">
        {% render 'how-it-works-content', variant: 'modal' %}
      </div>
      
      <div class="how-it-works-modal__actions">
        <button 
          type="button" 
          class="button button--primary how-it-works-modal__close-btn"
          ref="closeButton2"
          on:click="/closeDialog"
        >
          {{ 'preorder.how_it_works.got_it' | t: default: 'Got it' }}
        </button>
      </div>
    </div>
  </dialog>
</how-it-works-dialog>

{% stylesheet %}
  .how-it-works-modal {
    padding: 0;
    border: var(--style-border-popover);
    height: fit-content;
    width: min(90vw, 600px);
    max-height: 90vh;
    overflow: hidden;
    box-shadow: 0 5px 30px rgba(0, 0, 0, 0.15);
    border-radius: var(--border-radius);

    @media screen and (max-width: 750px) {
      width: 95vw;
      max-width: none;
      border-radius: var(--border-radius);
    }
  }

  .how-it-works-modal[open] {
    display: flex;
    flex-direction: column;
    animation: modalSlideInTop var(--animation-speed) var(--animation-easing) forwards;
  }

  .how-it-works-modal.dialog-closing {
    animation: modalSlideOutTop var(--animation-speed) var(--animation-easing) forwards;
  }

  .how-it-works-modal__close {
    position: absolute;
    top: var(--margin-2xs);
    right: var(--margin-2xs);
    width: var(--minimum-touch-target);
    height: var(--minimum-touch-target);
    color: var(--color-foreground);
    background-color: var(--color-background);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    z-index: 2;
    transition: transform 0.15s var(--animation-timing-bounce), opacity 0.15s var(--animation-easing);
  }

  .how-it-works-modal__close:active {
    transform: scale(0.8);
  }

  .how-it-works-modal__close svg {
    width: var(--icon-size-xs);
    height: var(--icon-size-xs);
  }

  .how-it-works-modal__content {
    display: flex;
    flex-direction: column;
    padding: var(--padding-xl);
    padding-top: var(--padding-3xl);
    gap: var(--gap-lg);
    overflow-y: auto;
    flex: 1;
  }

  .how-it-works-modal__header {
    text-align: center;
    border-bottom: 1px solid var(--color-border);
    padding-bottom: var(--padding-md);
  }

  .how-it-works-modal__title {
    margin: 0;
    font-size: var(--font-size--xl);
    font-weight: 600;
    color: var(--color-foreground);
  }

  /* Modal-specific overrides for the reusable content */
  .how-it-works-modal .how-it-works-content__steps {
    display: flex;
    flex-direction: column;
    gap: var(--gap-lg);
  }

  .how-it-works-modal .how-it-works-content__step {
    display: flex;
    gap: var(--gap-md);
    align-items: flex-start;
  }

  .how-it-works-modal .how-it-works-content__step-icon {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: var(--color-background);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: var(--margin-xs);
  }

  .how-it-works-modal .how-it-works-content__step-content {
    flex: 1;
  }

  .how-it-works-modal .how-it-works-content__step-title {
    margin: 0 0 var(--margin-xs) 0;
    font-size: var(--font-size--md);
    font-weight: 600;
    color: var(--color-foreground);
  }

  .how-it-works-modal .how-it-works-content__step-description {
    margin: 0;
    font-size: var(--font-size--sm);
    line-height: 1.4;
    color: var(--color-foreground-75);
  }

  .how-it-works-modal .how-it-works-content__note {
    display: flex;
    gap: var(--gap-sm);
    padding: var(--padding-md);
    background-color: var(--color-background-2);
    border-radius: var(--border-radius);
    border-left: 4px solid var(--color-accent);
    margin-top: var(--margin-md);
  }

  .how-it-works-modal .how-it-works-content__note-icon {
    flex-shrink: 0;
    color: var(--color-accent);
    margin-top: var(--margin-2xs);
  }

  .how-it-works-modal .how-it-works-content__note-content {
    flex: 1;
  }

  .how-it-works-modal .how-it-works-content__note-content p {
    margin: 0;
    font-size: var(--font-size--sm);
    line-height: 1.4;
    color: var(--color-foreground-75);
  }

  .how-it-works-modal__actions {
    display: flex;
    justify-content: center;
    margin-top: var(--margin-md);
    border-top: 1px solid var(--color-border);
    padding-top: var(--padding-md);
  }

  .how-it-works-modal__close-btn {
    padding: var(--padding-md) var(--padding-xl);
    border-radius: var(--border-radius);
    font-weight: 500;
    transition: all 0.2s var(--animation-easing);
    background-color: var(--color-button);
    color: var(--color-button-text);
    border: none;
    min-width: 120px;
  }

  .how-it-works-modal__close-btn:hover {
    background-color: var(--color-button-hover);
  }
{% endstylesheet %}