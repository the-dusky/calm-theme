{% comment %}
  Preorder Information Card
  
  Displays preorder information and "How it works" button
  Replaces the buy buttons section for preorder/drop products
  
  @param {product} product - The product object
  @param {collection} drop_collection - The collection with drop metafields
  @param {block} block - The block settings for styling
{% endcomment %}

{% liquid
  # Format the ship by date - will be updated by drop selector
  assign ship_by_date = drop_collection.metafields.custom.ship_by_date
  assign formatted_ship_date = 'TBD'
  if ship_by_date != blank
    assign formatted_ship_date = ship_by_date | date: '%B %d, %Y'
  endif
  
  # Format drop location if available
  assign drop_location = drop_collection.metafields.custom.drop_location
  assign location_text = ''
  if drop_location != blank
    assign location_text = ' (' | append: drop_location | append: ')'
  endif
  
  # Check if product has multiple drops
  assign drop_collections = product.collections | where: 'metafields.custom.ship_by_date', present
  assign has_multiple_drops = false
  if drop_collections.size > 1
    assign has_multiple_drops = true
  endif
%}

<div class="preorder-info-card layout-panel-flex layout-panel-flex--column">
  <div class="preorder-info-card__content">
    <div class="preorder-info-card__icon">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"/>
        <path d="M12 6v6l4 2" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>
    
    <div class="preorder-info-card__text">
      <h3 class="preorder-info-card__title">
        {{ 'preorder.info_card.title' | t: default: 'This is a preorder item' }}{{ location_text }}
        {% if has_multiple_drops %}
          <span class="preorder-info-card__drop-indicator">(Multiple drops available)</span>
        {% endif %}
      </h3>
      <p class="preorder-info-card__description">
        {% assign description_text = 'preorder.info_card.description' | t: ship_date: formatted_ship_date %}
        {% if description_text contains 'Translation missing' %}
          I will ship out to customers on or before <span data-ship-date>{{ formatted_ship_date }}</span>.
        {% else %}
          {{ description_text | replace: formatted_ship_date, '<span data-ship-date>' | append: formatted_ship_date | append: '</span>' }}
        {% endif %}
      </p>
      {% if has_multiple_drops %}
        <p class="preorder-info-card__drop-hint">
          <small>💡 Use the drop selector above to choose your preferred shipping option</small>
        </p>
      {% endif %}
    </div>
  </div>
  
  <div class="preorder-info-card__actions">
    <button 
      type="button" 
      class="preorder-info-card__how-it-works-btn"
      onclick="openHowItWorksModal()"
    >
      {{ 'preorder.info_card.how_it_works' | t: default: 'How it works' }}
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M7 17L17 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M7 7h10v10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
    
    <a 
      href="/pages/how-it-works" 
      class="preorder-info-card__learn-more-link"
    >
      {{ 'preorder.info_card.learn_more' | t: default: 'Learn more' }}
    </a>
  </div>
</div>

<script>
  function openHowItWorksModal() {
    const howItWorksDialog = document.getElementById('how-it-works-dialog');
    if (howItWorksDialog && typeof howItWorksDialog.showDialog === 'function') {
      howItWorksDialog.showDialog();
    }
  }
</script>

{% stylesheet %}
  .preorder-info-card {
    position: relative;
    transition: transform var(--hover-transition-duration) var(--hover-transition-timing),
      box-shadow var(--hover-transition-duration) var(--hover-transition-timing);
    will-change: transform, box-shadow;
    z-index: var(--layer-flat);
    gap: var(--padding-md);
    margin-top: var(--padding-lg);
    background-color: #fef3c7;
    border: 1px solid #f59e0b;
    padding: var(--padding-lg);
  }

  .preorder-info-card__content {
    display: flex;
    align-items: flex-start;
    gap: var(--padding-md);
  }

  .preorder-info-card__icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: #3b82f6;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .preorder-info-card__text {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--padding-3xs);
  }

  .preorder-info-card__title {
    margin: 0;
    font-size: var(--font-size--md);
    font-weight: 600;
    color: #1e40af;
    line-height: 1.3;
  }

  .preorder-info-card__description {
    margin: 0;
    font-size: var(--font-size--sm);
    color: #1e40af;
    line-height: 1.4;
    opacity: 0.8;
  }

  .preorder-info-card__drop-indicator {
    font-size: var(--font-size--xs);
    font-weight: 400;
    color: #059669;
    background-color: #d1fae5;
    padding: 2px 6px;
    border-radius: 4px;
    margin-left: 8px;
  }

  .preorder-info-card__drop-hint {
    margin: var(--padding-xs) 0 0 0;
    font-size: var(--font-size--xs);
    color: #059669;
    background-color: #ecfdf5;
    padding: var(--padding-xs);
    border-radius: 4px;
    border-left: 3px solid #10b981;
  }

  [data-ship-date] {
    font-weight: 600;
    color: #1e40af;
  }

  .preorder-info-card__actions {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: var(--padding-md);
  }

  .preorder-info-card__how-it-works-btn {
    background: none;
    border: 1px solid #3b82f6;
    color: #1e40af;
    padding: var(--padding-sm) var(--padding-md);
    border-radius: var(--border-radius);
    font-size: var(--font-size--sm);
    font-weight: 500;
    cursor: pointer;
    transition: all var(--hover-transition-duration) var(--hover-transition-timing);
    display: inline-flex;
    align-items: center;
    gap: var(--padding-xs);
    text-decoration: none;
  }

  .preorder-info-card__how-it-works-btn:hover {
    background-color: #3b82f6;
    border-color: #1e40af;
    color: white;
    transform: translateY(-1px);
  }

  .preorder-info-card__learn-more-link {
    color: #1e40af;
    text-decoration: underline;
    font-size: var(--font-size--sm);
    font-weight: 500;
    transition: color var(--hover-transition-duration) var(--hover-transition-timing);
  }

  .preorder-info-card__learn-more-link:hover {
    color: #3b82f6;
    text-decoration: none;
  }

  @media screen and (max-width: 750px) {
    .preorder-info-card__content {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .preorder-info-card__actions {
      justify-content: center;
      flex-direction: column;
      gap: var(--padding-sm);
    }
  }
{% endstylesheet %}