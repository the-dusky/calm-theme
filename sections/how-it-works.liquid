<div class="how-it-works-section">
  <div class="container">
    <h2>{{ section.settings.heading | default: 'Your Order Journey' }}</h2>
    <p>{{ section.settings.subtitle | default: 'Here is what to expect with your international shipment' }}</p>
  </div>
</div>

{% schema %}
{
  "name": "How It Works",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Main Heading",
      "default": "Your Order Journey"
    },
    {
      "type": "text",
      "id": "subtitle", 
      "label": "Subtitle",
      "default": "Here is what to expect with your international shipment"
    }
  ],
  "presets": [
    {
      "name": "How It Works",
      "category": "t:categories.storytelling"
    }
  ]
}
{% endschema %}